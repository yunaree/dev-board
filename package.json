{
  "name": "dev-board",
  "private": true,
  "scripts": {
    "f:dev": "cd packages/frontend && next dev --turbopack",
    "f:build": "cd packages/frontend && pnpm run build",
    "f:start": "cd packages/frontend && pnpm run start"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@nestjs/passport": "^11.0.5",
    "passport": "^0.7.0",
    "passport-github2": "^0.1.12",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/passport-github2": "^1.2.9"
  }
}
